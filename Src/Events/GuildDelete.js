const { prefix } = require("../Credentials/Config");
const commandOptionsProcessor = require("../Structures/CommandOptions/Processor");
const { shuffle, sendServers, repeatSendServers, sendScheduledMessages, guildLeave } = require("../../function");

module.exports = {
    name: "guildDelete",
    run: async(guild, client) => {
     

    }
};
